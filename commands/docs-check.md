---
description: Validate docs/ structure (read-only) — run the deterministic validator and explain each failure
---

Invoke the `docs-kit:docs-check` skill with the Skill tool and follow it exactly.
Remember: report only, never fix. User arguments, if any: $ARGUMENTS
