---
description: Scaffold the three-layer docs structure (14 folders + templates + README) into this repo
---

Invoke the `docs-kit:docs-init` skill with the Skill tool and follow it exactly —
do not improvise the scaffold by hand. User arguments, if any: $ARGUMENTS
