---
id: BACKLOG-000
description: "EXAMPLE — ship the shared structured-logging helper and migrate one service"
source_ref: DECISION-000
status: done
---

# BACKLOG-000 — Example: structured logging rollout, phase 1

> Part of the `-000` worked example chain. Because this item is `status: done`,
> `92_audit/LOG.md` carries a matching example line — that is the completion
> trigger every real Backlog item must follow.

## Notes

- Full lane ⇒ `source_ref` points to the **Decision**. Fast lane items point to
  the **Issue** instead.
- `status` moves `open → in-progress → done`. On `done`, append one line to
  `92_audit/` in the same session.
