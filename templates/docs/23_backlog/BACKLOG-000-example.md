---
id: BACKLOG-000
description: "EXAMPLE — làm xong logging helper dùng chung và migrate một service"
source_ref: DECISION-000
status: done
---

# BACKLOG-000 — Example: structured logging rollout, phase 1

> Thuộc chuỗi ví dụ `-000`. Vì item này đang `status: done`, `92_audit/LOG.md` có
> một dòng ví dụ tương ứng — đó chính là trigger hoàn thành mà mọi Backlog item
> thật đều phải theo.

## Notes

- Full lane ⇒ `source_ref` trỏ tới **Decision**. Item đi fast lane thì trỏ tới
  **Issue**.
- `status` đi theo `open → in-progress → done`. Khi tới `done`, nối thêm một dòng
  vào `92_audit/` ngay trong phiên đó.
