---
id: DECISION-000
proposal_ref: PROPOSAL-000
outcome: approved
reason: "Log máy parse được sẽ mở đường cho việc truy vết sự cố; chi phí migrate bị chặn trên nhờ helper dùng chung"
decided_by: "example-owner"
architecture_amendment: none
---

# DECISION-000 — Example: approve structured logging

> Thuộc chuỗi ví dụ `-000`.

## Outcome

Duyệt đúng như đề xuất (phương án 1 của PROPOSAL-000).

## Consequences

- Tạo một Backlog item: BACKLOG-000 (`source_ref: DECISION-000`).
- `architecture_amendment: none` → không sửa `02_architecture/`. Khi một Decision
  **có** bắt sửa, ghi tóm tắt một dòng vào trường đó rồi sửa `02_architecture/`
  (phần body + thêm dòng `amended_by`) ngay trong phiên nó được duyệt.
